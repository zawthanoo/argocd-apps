apiVersion: v1
kind: Service
metadata:
  name: angular-mfe-shell
  labels:
    app: angular-mfe-shell  
spec:
  selector:
    app: angular-mfe-shell
  type: ClusterIP
  sessionAffinity: None
  ports:
    - name: 8080-tcp
      port: 8080
      protocol: TCP
      targetPort: 8080
