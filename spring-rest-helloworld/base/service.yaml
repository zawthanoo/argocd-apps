apiVersion: v1
kind: Service
metadata:
  name: spring-rest-helloworld
  labels:
    app: spring-rest-helloworld  
spec:
  selector:
    app: spring-rest-helloworld
  type: ClusterIP
  sessionAffinity: None
  ports:
    - name: 8282-tcp
      port: 8282
      protocol: TCP
      targetPort: 8282
